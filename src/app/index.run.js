(function () {
  'use strict';
  //TODO: change this code on angular route resolve
  angular
    .module('yandexMarketReview')
    .run(runBlock);

  /** @ngInject */
  function runBlock($http, $rootScope, $state, $window, apiUrl) {
  }
})();
